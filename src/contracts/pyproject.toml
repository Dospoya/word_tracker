[project]
name = "longbutton-contracts"
version = "0.1.0"
requires-python = ">=3.12.0"
dependencies = [
    "pydantic==2.5.3",
    "pydantic-settings==2.2.1",
    "typing-extensions>=4.12.2",
]
[tool.setuptools.packages.find]
where = ["."]
include = [
    "contracts*"
]
